# Dependencies & build output
node_modules/
dist/
.astro/

# Generated wasm (copied from node_modules by scripts/copy-draco-wasm — reproducible, not source)
public/draco/
public/mv-draco/

# Spike throwaway sample model (regenerated by scripts/make-sample) — removed at T12
public/sample.glb

# Test/automation artifacts
.playwright-mcp/
spike-gate-result.png
test-results/
playwright-report/

# Gitignored local copy of helmet-livery reference files (never edited, never committed)
reference/

# Stray models at the repo root (local test inputs). The committed demo lives in
# public/demo/ and must be CC0/CC-BY — see public/demo/ATTRIBUTION.md.
/*.glb
/*.gltf
